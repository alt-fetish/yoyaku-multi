# spec.md — ALT-FETISH 交流セッション管理システム

## サービス概要

ALT-FETISHが提供するラバー着用体験セッションの管理Webアプリケーション。
管理者（Ryotaro）が開催日を先に設定し、参加希望者を募ることでセッションを成立させる。

通常の1対1セッション予約（Airリザーブ管理）とは完全に分離した導線を持つ。

---

## 利用者・役割

| 役割 | 説明 |
|------|------|
| 管理者（Ryotaro） | 開催日設定・参加者選定・セッション確定・履歴管理 |
| 参加希望者 | 開催日への参加表明・NG行為の事前設定・結果の受領 |

---

## 開催枠

各開催枠は日付・開始時刻・終了時刻・定員で管理する。
以前の `solo` / `group` 固定定員は使用せず、申込み可否と残枠表示は `capacity` を基準に判定する。

---

## 参加資格

- 成人男性のみ
- 複数名開催への参加可否は管理者が個別に判断
- 初回申込時に管理者が資格確認（チャット経由で事前コミュニケーション可）

---

## 核心となる運用フロー

```
1. 管理者が「開催日」を登録（日付・時刻・定員）
        ↓
2. 登録者に一斉通知メール送信（任意）
        ↓
3. 参加希望者が開催日一覧を見て「参加したい」ボタンを押す
        ↓
4. 管理者が参加表明者の一覧とNG設定を確認
        ↓
5. 管理者が参加者を選定して「確定」→ 両者に通知メール
        ↓
6. セッション実施 → ステータスを「done」に更新
```

マッチングアルゴリズムは存在しない。管理者が手動で選定する。

---

## 機能要件

### 参加希望者側

#### 1. 登録フォーム（初回のみ）
- ハンドルネーム
- メールアドレス
- NG行為チェックリスト（下記参照）
- 送信後、URLトークン付きの確認メールを受信（アカウント不要）

#### 2. NG行為チェックリスト

以下の各行為について「OK / NG」を個別に選択する。  
**デフォルトはすべてNG**とし、許可するものだけOKに変更する設計。

- 指を口に入れる
- アナルに触れる
- シース（陰茎部ラバー）を外す
- キス
- フェラチオ

#### 3. 開催日一覧ページ（`/events`）
- 公開中の開催日一覧を表示（日時・定員・残枠）
- 定員に達した開催枠は `SOLD OUT` 表示にして申込み不可
- 「参加したい」ボタンで参加表明（トークン認証）
- 参加表明の取消も可能（セッション確定前まで）

#### 4. マイページ（`/my/:token`）
- 自分の参加表明一覧と各ステータス
- NG設定の確認
- 管理者とのチャット欄

#### 5. メール通知
- 登録完了メール（マイページURL付き）
- 参加確定通知メール（日時・場所）
- セッション前日リマインダー（同意再確認）
- キャンセル通知メール

---

### 管理者側

#### 1. 開催日管理（`/admin/events`）

**一覧**
- 開催予定日の一覧（日時・定員・参加表明数・ステータス）
- ステータス：`open`（募集中）/ `confirmed`（確定）/ `done`（実施済）/ `cancelled`
- ページ上部に「＋ セッション登録」ボタンを設置 → 手動新規作成フォームへ遷移
- 隣に「📎 CSVアップロード」ボタンを設置 → CSVアップロード画面へ遷移

**新規作成（手動）**
- 日付・開始時刻・終了時刻・定員・メモ
- 作成後、一斉通知メールの送信有無を選択

**CSVアップロード（一括登録）**
- フォーマット：`date,starttime,endtime,capacity`（ヘッダー行あり）
- 日付形式：`YYYY-MM-DD`
- 時刻形式：`HH:MM`
- capacity：1以上の整数
- アップロード後、プレビュー画面で内容確認してから確定
- 重複チェック：同一日付・同一開始時刻が既に登録済みの場合はその行をスキップしてエラー表示
- 正常行のみDBに登録（部分成功あり）
- `endtime` はeventsテーブルには持たず、メモ欄に記録

**編集・削除**
- 日時・メモの変更
- 削除は `open` 状態のみ可（`confirmed` 以降は不可）

#### 2. 参加表明者一覧（`/admin/events/:id/entries`）
- 当該開催日への参加表明者の一覧
- 各参加者のNG設定を表示（管理者のみ閲覧）
- 2名選択時にNG設定の相互比較ビューを表示
  - 両者OK / 片方のみNG / 両者NG を色分け表示

#### 3. 参加者確定操作
- 参加者を選択して「確定」ボタン
- 確定と同時に参加者へ通知メール送信
- 開催日ステータスを `confirmed` に更新
- 未選定の表明者には「今回は見送り」通知（任意）

#### 4. 参加希望者管理（`/admin/applicants`）
- 登録者の一覧（ハンドル・メール・登録日・参加回数）
- NG設定の閲覧
- 個別チャットへのリンク

#### 5. チャット（管理者 ↔ 参加者 1対多）
- 管理者は各参加者と個別チャット
- 参加者同士は互いの存在・内容を見えない
- 未読バッジを参加者一覧に表示
- htmxポーリング（10秒間隔）で擬似リアルタイム

#### 6. セッション履歴 CRUD（`/admin/sessions`）
- 実施済みセッションの記録・閲覧・編集・削除
- 管理者専用（参加者には非公開）
- `done` に更新時に自動生成、手動登録も可

---

## セッション履歴データ構造

開催日が `done` になると自動的に履歴レコードが生成される。
管理者は手動での新規登録・編集・削除も可能。

| 項目 | 内容 |
|------|------|
| セッションID | 自動生成UUID |
| 定員 | 開催枠の capacity |
| 参加者 | applicantsと多対多（中間テーブル） |
| 日時 | session_date / session_time |
| 実施確定日時 | completed_at（doneに変更時に自動記録） |
| ステータス | confirmed / done / cancelled |
| メモ | 管理者内部メモ（非公開） |

---

## 非機能要件

- 参加者のNG設定は管理者のみ閲覧可能
- 参加者同士はお互いのNG設定・存在を知ることができない
- 管理者画面はセッション認証（Cookie）
- 参加者側はURLトークンベース（アカウント作成不要）
- メールアドレス等の個人情報はサービス外に漏洩しない

---

## スコープ外

- 通常1対1セッションの予約管理（Airリザーブが担当）
- 決済機能
- 参加者同士のチャット・メッセージング
- 自動マッチングアルゴリズム
- 開催日の自動生成・繰り返し設定
